#version 330

layout (location = 0) in vec4 vertex;
out vec2 out_uv;

void main(void) {

  // Place text on the eye of camera
  gl_Position = vec4(vertex.xy, 0.0, 1);

  // Output uv coordinates
  out_uv = vertex.zw;
}